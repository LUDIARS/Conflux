---
task: "cc-api-cf-spawn"
project: "opus-mid"
kind: "実装"
created: "2026-09-18"
---
# Cc 側に必要な追加 API (Cf ビルド/spawn/役職連携)

## 目的
Cf からの spawn と idempotency_key 照会、ビルド要求 (dedupe_key)、Cc フック→Cf POST /api/hooks/build-events と /api/hooks/integration の配線と共有鍵 (CONFLUX_HOOK_TOKEN)、本人/役職照会。仕様は spec/architecture/cc-integration.md。Cf 側は設定パス未設定の間 not_connected を返す。

## 完了条件
- Cc 側に必要な追加 API (Cf ビルド/spawn/役職連携) が完了している。

## スコープ (編集可ディレクトリ)
