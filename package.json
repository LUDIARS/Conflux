{
  "name": "@ludiars/conflux",
  "version": "0.1.0",
  "private": true,
  "description": "Conflux (Cf): CDGD flow workspace — tides/variants graph, play feedback, adoption decisions, Cc spawn, playable results.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/main.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
